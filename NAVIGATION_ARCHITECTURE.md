# Navigation Architecture Diagram

## Complete Page Structure

```
┌─────────────────────────────────────────────────────────────┐
│                    LAYOUT (app/layout.tsx)                  │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │              HEADER (Fixed, z-index: 50)                │ │
│ │ ┌─────────────────────────────────────────────────────┐ │ │
│ │ │ [Logo] Features | DiTeLe | Curriculum | Preise | FAQ│ │ │
│ │ │                                    [Jetzt starten →]│ │ │
│ │ └─────────────────────────────────────────────────────┘ │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                               │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │                   PAGE CONTENT                          │ │
│ │                                                         │ │
│ │  ┌────────────────────────────────────────────────┐   │ │
│ │  │  HERO SECTION (no ID)                         │   │ │
│ │  │  • Main headline & CTA                         │   │ │
│ │  │  • Stats cards (Kurs, DiTeLe, Erfolg)        │   │ │
│ │  └────────────────────────────────────────────────┘   │ │
│ │                                                         │ │
│ │  ┌────────────────────────────────────────────────┐   │ │
│ │  │  📍 DITELE DEMO (id="ditele-demo")            │   │ │
│ │  │  • Interactive quiz question                   │   │ │
│ │  │  • Real-time feedback                         │   │ │
│ │  └────────────────────────────────────────────────┘   │ │
│ │           ↑                                            │ │
│ │           │ Header: "DiTeLe Demo" → #ditele-demo      │ │
│ │           │ Footer: "DiTeLe Demo" → #ditele-demo      │ │
│ │                                                         │ │
│ │  ┌────────────────────────────────────────────────┐   │ │
│ │  │  📍 COMPARISON (id="features")                 │   │ │
│ │  │  • Self-study vs Seminar vs Our System        │   │ │
│ │  │  • Feature comparison table                   │   │ │
│ │  └────────────────────────────────────────────────┘   │ │
│ │           ↑                                            │ │
│ │           │ Header: "Features" → #features             │ │
│ │           │ Footer: "Features" → #features             │ │
│ │                                                         │ │
│ │  ┌────────────────────────────────────────────────┐   │ │
│ │  │  DITELE DEEP DIVE (no ID)                     │   │ │
│ │  │  • Detailed DiTeLe features                   │   │ │
│ │  │  • Screenshots and examples                   │   │ │
│ │  └────────────────────────────────────────────────┘   │ │
│ │                                                         │ │
│ │  ┌────────────────────────────────────────────────┐   │ │
│ │  │  📍 CURRICULUM (id="curriculum")              │   │ │
│ │  │  • 4-week course timeline                     │   │ │
│ │  │  • Module breakdown with progress             │   │ │
│ │  └────────────────────────────────────────────────┘   │ │
│ │           ↑                                            │ │
│ │           │ Header: "Curriculum" → #curriculum         │ │
│ │           │ Footer: "Curriculum" → #curriculum         │ │
│ │                                                         │ │
│ │  ┌────────────────────────────────────────────────┐   │ │
│ │  │  📍 PRICING (id="pricing")                    │   │ │
│ │  │  • €497 package details                       │   │ │
│ │  │  • Payment options (1x or 5x €100)           │   │ │
│ │  │  • Money-back guarantee*                      │   │ │
│ │  └────────────────────────────────────────────────┘   │ │
│ │           ↑                                            │ │
│ │           │ Header: "Preise" → #pricing                │ │
│ │           │ Footer: "Preise" → #pricing                │ │
│ │           │ Header CTA: "Jetzt starten" → #pricing     │ │
│ │                                                         │ │
│ │  ┌────────────────────────────────────────────────┐   │ │
│ │  │  📍 FAQ (id="faq")                            │   │ │
│ │  │  • 10 accordion questions                     │   │ │
│ │  │  • Expandable answers                         │   │ │
│ │  └────────────────────────────────────────────────┘   │ │
│ │           ↑                                            │ │
│ │           │ Header: "FAQ" → #faq                       │ │
│ │           │ Footer: "FAQ" → #faq                       │ │
│ │                                                         │ │
│ │  ┌────────────────────────────────────────────────┐   │ │
│ │  │  FINAL CTA (no ID)                            │   │ │
│ │  │  • Final conversion section                   │   │ │
│ │  │  • CTA button → #pricing                      │   │ │
│ │  └────────────────────────────────────────────────┘   │ │
│ │                                                         │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                               │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │                    FOOTER                               │ │
│ │ ┌─────────┬──────────┬──────────┬────────────────────┐ │ │
│ │ │ BRAND   │ PRODUKT  │RECHTLICH │ KONTAKT & SOCIAL   │ │ │
│ │ │         │          │          │                    │ │ │
│ │ │ [W]     │ Preise   │Impressum │ info@test-it-...   │ │ │
│ │ │ WAMOCON │ Features │Datenschutz│ +49 6196 5838312  │ │ │
│ │ │ Academy │ DiTeLe   │ AGB      │                    │ │ │
│ │ │         │Curriculum│          │ Instagram YouTube  │ │ │
│ │ │         │ FAQ      │          │ Facebook           │ │ │
│ │ └─────────┴──────────┴──────────┴────────────────────┘ │ │
│ │                                                         │ │
│ │ ────────────────────────────────────────────────────── │ │
│ │                                                         │ │
│ │  © 2026 WAMOCON Academy GmbH. Alle Rechte vorbehalten │ │
│ │  ISTQB® Disclaimer                                     │ │
│ │                                                         │ │
│ │  Zum Kurs • Datenschutz • Impressum • AGB             │ │
│ └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

## Navigation Flow Diagram

```
User Click Flow:
═══════════════

HEADER NAVIGATION
┌──────────────────┐
│   Click "Features"│
└─────────┬─────────┘
          ↓
    querySelector('#features')
          ↓
    Calculate offset (80px)
          ↓
    window.scrollTo({ smooth })
          ↓
┌─────────────────────────┐
│ Scroll to Comparison    │
│ Section (id="features") │
└─────────────────────────┘
          ↓
    IntersectionObserver detects
          ↓
┌─────────────────────────┐
│ Highlight "Features" in │
│ Header (active state)   │
└─────────────────────────┘


MOBILE MENU
┌──────────────────┐
│  Click Hamburger │
└─────────┬─────────┘
          ↓
    setIsMobileMenuOpen(true)
          ↓
┌─────────────────────────┐
│ Lock body scroll        │
│ Show backdrop + menu    │
└─────────┬───────────────┘
          ↓
    User clicks nav item
          ↓
    Smooth scroll to section
          ↓
    setIsMobileMenuOpen(false)
          ↓
┌─────────────────────────┐
│ Unlock body scroll      │
│ Hide menu               │
└─────────────────────────┘


FOOTER NAVIGATION
┌──────────────────┐
│ Click "Curriculum"│
└─────────┬─────────┘
          ↓
    event.preventDefault()
          ↓
    handleScrollClick('#curriculum')
          ↓
    querySelector('#curriculum')
          ↓
    Calculate offset (80px)
          ↓
    window.scrollTo({ smooth })
          ↓
┌─────────────────────────┐
│ Scroll to Curriculum    │
│ Section (id="curriculum")│
└─────────────────────────┘
```

## Component Interaction Map

```
┌────────────────────────────────────────────────────────┐
│                    LAYOUT COMPONENT                    │
│                                                        │
│  Imports: Header, Footer                              │
│  Renders: <Header /> {children} <Footer />            │
└───────────────┬────────────────────────┬───────────────┘
                │                        │
        ┌───────▼───────┐        ┌───────▼────────┐
        │    HEADER     │        │     FOOTER     │
        │               │        │                │
        │ State:        │        │ Handlers:      │
        │ • isScrolled  │        │ • handleScroll │
        │ • isMobile    │        │ • handleClick  │
        │ • activeSection│       │                │
        │               │        │ Data:          │
        │ Effects:      │        │ • productLinks │
        │ • scroll      │        │ • legalLinks   │
        │ • observer    │        │ • socialLinks  │
        │ • resize      │        │                │
        │ • body lock   │        │                │
        └───────┬───────┘        └───────┬────────┘
                │                        │
                └────────────┬───────────┘
                             │
                    ┌────────▼────────┐
                    │  PAGE SECTIONS  │
                    │                 │
                    │ • Hero          │
                    │ • DiTeleDemo    │
                    │ • Comparison    │
                    │ • DeepDive      │
                    │ • Curriculum    │
                    │ • Pricing       │
                    │ • FAQ           │
                    │ • FinalCTA      │
                    └─────────────────┘
```

## Data Flow

```
SCROLL EVENT FLOW:
═════════════════

Window Scroll Event
        ↓
Header useEffect (scroll listener)
        ↓
Calculate scrollY position
        ↓
setIsScrolled(position > 50)
        ↓
Header className updates
        ↓
CSS transitions apply
        ↓
Sticky header style changes
(transparent → backdrop blur + shadow)


INTERSECTION OBSERVER FLOW:
═══════════════════════════

Sections enter viewport
        ↓
IntersectionObserver callback
        ↓
entry.isIntersecting === true
        ↓
setActiveSection(entry.target.id)
        ↓
Header nav items re-render
        ↓
Active item gets highlighted style
(text-accent + bg-accent/10)


NAVIGATION CLICK FLOW:
═════════════════════

User clicks nav button
        ↓
handleNavClick(item)
        ↓
if (item.type === 'scroll')
        ↓
querySelector(item.href)
        ↓
Calculate element position
        ↓
Subtract header offset (80px)
        ↓
window.scrollTo({ smooth })
        ↓
Browser smooth scrolls to position
        ↓
IntersectionObserver detects
        ↓
Active section updates
```

## State Management

```
HEADER STATE:
═════════════

┌─────────────────────────────────────┐
│ isScrolled: boolean                 │
│ • Purpose: Sticky header styling   │
│ • Updated: On scroll event          │
│ • Effect: Background & shadow       │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│ isMobileMenuOpen: boolean           │
│ • Purpose: Mobile menu visibility   │
│ • Updated: On hamburger click       │
│ • Effect: Menu slide + body lock    │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│ activeSection: string               │
│ • Purpose: Highlight current section│
│ • Updated: By IntersectionObserver  │
│ • Effect: Nav item active styling   │
└─────────────────────────────────────┘


FOOTER STATE:
═════════════

Stateless component - uses callbacks only
• handleScrollClick: Memoized with useCallback
• handleLinkClick: Distinguishes link types
```

## Performance Optimizations

```
HEADER OPTIMIZATIONS:
═══════════════════════

✓ Passive event listeners
  addEventListener('scroll', handler, { passive: true })
  → Improves scroll performance

✓ useCallback for handlers
  → Prevents unnecessary re-renders

✓ Cleanup functions
  → Removes listeners on unmount

✓ Conditional rendering
  → Mobile menu only renders when open


FOOTER OPTIMIZATIONS:
═══════════════════════

✓ useCallback for scroll handler
  → Stable reference, no re-creation

✓ Static data arrays
  → Defined outside component

✓ Conditional scroll prevention
  → Only preventDefault for scroll type


LAYOUT OPTIMIZATIONS:
═══════════════════════

✓ Static generation
  → All pages pre-rendered at build

✓ Code splitting by route
  → Legal pages load separately

✓ Shared chunks
  → 102 kB reused across pages
```

## TypeScript Interfaces

```typescript
// Header
interface NavItem {
  id: string         // Unique identifier
  label: string      // Display text
  href: string       // Target (e.g., '#pricing')
  type: 'link' | 'button' | 'scroll'
}

// Footer
interface FooterLink {
  id: string         // Unique identifier
  label: string      // Display text
  href: string       // Target URL or hash
  type: 'scroll' | 'link' | 'external'
  ariaLabel?: string // Accessibility label
}

interface SocialLink {
  platform: string   // Social media name
  url: string        // External URL
  ariaLabel: string  // Screen reader text
}
```

## Complete Navigation Mapping

```
┌─────────────────────────────────────────────────────────┐
│                   NAVIGATION MATRIX                     │
├──────────────┬───────────┬─────────────┬───────────────┤
│ Section Name │ HTML ID   │ Header Link │ Footer Link   │
├──────────────┼───────────┼─────────────┼───────────────┤
│ Comparison   │ features  │ Features    │ Features      │
│ DiTeLe Demo  │ditele-demo│ DiTeLe Demo │ DiTeLe Demo   │
│ Curriculum   │curriculum │ Curriculum  │ Curriculum    │
│ Pricing      │ pricing   │ Preise      │ Preise        │
│ FAQ          │ faq       │ FAQ         │ FAQ           │
├──────────────┴───────────┴─────────────┴───────────────┤
│ Legal Pages (not sections):                            │
│ • /impressum  → Footer "Impressum"                     │
│ • /datenschutz → Footer "Datenschutz"                  │
│ • /agb        → Footer "AGB"                           │
└─────────────────────────────────────────────────────────┘
```

---

**Legend:**
- 📍 = Section with navigation ID
- ↑ = Navigation link pointing to section
- → = Click action / navigation flow
